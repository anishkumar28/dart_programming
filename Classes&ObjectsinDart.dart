import 'dart:io';

void main(){

var raman = new Human();// create a new object with Human() properties and store in variable raman 
}

class Human { // class
  Human();//object holding properties of the class Human
}